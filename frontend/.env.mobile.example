# Copy to .env.mobile for native store builds (vite --mode mobile)
# API host only — /api/v1 is appended in src/constants

VITE_API_URL=https://activehq-api.onrender.com
VITE_APP_URL=https://activehq.fit

# Optional
# VITE_SENTRY_DSN=
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
